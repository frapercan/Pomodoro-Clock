import Typography from "@mui/material/Typography";

function Ciclos(props) {
  return (

    <Typography variant="h3" gutterBottom>
        Ciclo {props.ciclo}

  </Typography>


  );
}

export default Ciclos;
